<div id="pagination" class="pagination">
  <% var prev_text = "&laquo; " + __('prev');var next_text = __('next') + " &raquo;"%>
  <%- paginator({
    prev_text,
    next_text,
    escape: false
  }) %>
</div>
